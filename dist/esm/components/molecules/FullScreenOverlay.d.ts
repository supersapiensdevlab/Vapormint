import { ReactNode } from "react";
type Props = {
    title: String;
    onClose: Function;
    children: ReactNode;
};
export default function FullScreenOverlay(props: Props): JSX.Element;
export {};
